/*
 * Achievement Messages
 *
 * This contains all the text for the Achievement component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Achievement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Achievement component!',
  },
});
