/*
 * RecentTasks Messages
 *
 * This contains all the text for the RecentTasks component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RecentTasks';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RecentTasks component!',
  },
});
