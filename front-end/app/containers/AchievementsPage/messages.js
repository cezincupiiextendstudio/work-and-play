/*
 * AchievementsPage Messages
 *
 * This contains all the text for the AchievementsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AchievementsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AchievementsPage container!',
  },
});
