/*
 * ChatPage Messages
 *
 * This contains all the text for the ChatPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ChatPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ChatPage container!',
  },
});
